/** 
 * 1. create a new project in console.google.com
 * 2. npm install firebase 
 * 3. create firebase-init- and getAuth to export auth 
 * 4. firebase settings > Authentication > enable email password authentication
 * 
 */

/**
 * hosting steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */

/**
 * git-hub repository url change to my github
 * git remote -v
 * copy and paste the new repository url
 * in addition of add set  git remote set-url 
 * then git add . 
 * then git commit -m
 * then git push 
 */